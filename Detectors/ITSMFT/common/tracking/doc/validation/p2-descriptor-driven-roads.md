# P2 descriptor-driven roads validation

The macro-off serial ITS/MFT suite passed after the final correction. The
combined composition test verifies graph-component road ordering remains
deterministic. Per-surface source ownership is staged atomically with the
loaded event and clears on reset; a uniformly foreign input source is
structural and leaves both publication exports empty. Macro-on configuration
remains blocked by the pre-existing unregistered native/frozen comparison
macro.

No replay is claimed: transition-local curvature preparation is candidate
physics and requires a separate fixture characterization campaign.
